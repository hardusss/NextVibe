# CLOCK IN Hackathon Changelog (Sep 8 – Oct 8, 2026)

All work below was built during the hackathon window. Format: date · scope · summary · key files.

## 2026-09-10
- fix(frontend): Seeker badge presentation — rounded-square in the style of X's organization badge (`borderRadius = size * 0.28`, Genesis Token artwork edge-to-edge via `contentFit:"cover"`, the 1px purple ring removed, no shadow/glow); tapping the badge on the profile header and the NFC/BLE tap-success cards opens a small bottom sheet in the house style (48px rounded-square artwork, "Seeker Verified" title, one source-aware line — on-chain detection vs `.skr` Seeker ID — and a Close button; `hitSlop 8`), while list badges stay non-tappable; deleted the "Seeker Verified · Genesis Token" line under the profile REP pill (badge next to the name is the only indicator); sizes normalized (profile header 20, tap cards 22, lists/rows 14–16 — comment thread bumped 13→14); every `UserBadges` row realigned to the username's optical center: rows `alignItems:'center'`, name gets `includeFontPadding:false` + `lineHeight = fontSize + 2` + `flexShrink:1` + `numberOfLines:1`, badge container `flexShrink:0, marginLeft:4` inside the shared component with all per-call-site margin/wrapper hacks removed (profile headers, feed, post details, comments, chat list/header, search, follows, recommendations, collectibles, post modal, event-connections "people you met"). Official checkmark icon/colour untouched. Profile pages now read `seeker_verified_source` from the existing user-detail payload for the sheet copy. Files: `frontend/NextVibe/components/Shared/UserBadges.tsx`, `components/ProfilePage/{ProfilePage.ios,ProfilePage.android,UserProfilePage,recommendateProfiles,CollectiblesModal,PostsList,FollowsTab,EventConnectionsSheet,PostModal}.tsx`, `components/Home/MainPage.{ios,android}.tsx`, `components/Comments/CommentPopup.tsx`, `components/PostDetails/{CommentThread,PostDetailsScreen}.tsx`, `components/Chat/{ChatItem,CustomChatScreen}.tsx`, `components/Search/SearchPage.{ios,android}.tsx`, `components/Events/EventNFC{Share,Receive}Screen.tsx`
- feat(backend): `grant_seeker_badges` management command — the Seeker Verified bootstrap, promoted from the throwaway gitignored script into `python manage.py grant_seeker_badges` (supersedes the "one-time script in scripts_tmp, delete after running" note below). Pass 1 grants `source='skr'` to users whose Seed Vault wallet label username ends in `.skr`; pass 2 runs the on-chain Genesis Token scan (`check_sgt_onchain` via nft-service, 24h cache) for users with a `wallet_address`. Flags: `--dry-run`, `--skr-only`, `--onchain-only`, `--limit N`, `--sleep 0.2` (seconds between Helius calls). Idempotent (already-verified users are skipped; safe to re-run), DB writes only (no pushes), one-token-one-account guard intact, summary line `skr_granted/onchain_granted/already_used/not_found/skipped`. No config in the command itself — nft-service URL and Helius key stay in constants/settings/env. Files: `backend/NextVibeAPI/user/management/commands/grant_seeker_badges.py`
- feat(seeker): Seeker Verified badge — a user whose connected wallet holds a Seeker Genesis Token (non-transferable Token-2022 NFT, one per Seeker device) gets an on-chain-verified badge shown next to the official checkmark everywhere a username appears. Backend: four new `User` fields (`seeker_verified`, unique `seeker_sgt_mint` for anti-sybil one-token-one-account, `seeker_verified_at`, `seeker_verified_source` 'onchain'|'skr'); `user/src/seeker_verification.py` (24h-cached check via nft-service, `grant_seeker_verified` with `SGT_ALREADY_USED` guard, `.skr`→onchain upgrade path, background daemon-thread verification with "You're Seeker Verified" push carrying `type: seeker_verified`); hooks after wallet sign-in (existing + new user) and `save-wallet/`; manual `POST /api/v1/users/seeker/verify/` (auth, `seeker_verify` 5/min throttle, cache-bypassing); `seeker_verified` exposed beside `official` in every serializer/dict (profile via `exclude`, search, recommended, history, readers/follows, feed `owner__seeker_verified`, post/comments, NFT menus `creator_seeker_verified`, event connections `is_seeker_verified`, chat list/members — which also gain the previously missing `official`). nft-service: `POST /seeker/sgt-check` implementing the official Solana Mobile algorithm (paginated `getTokenAccountsByOwnerV2` on Token-2022, zero-balance skip, batched `getMultipleAccountsInfo` + `unpackMint`/metadata-pointer/token-group-member checks against the GT2z…/GT22… authorities, returns the SGT mint); added `@solana/spl-token` + explicit `@solana/web3.js`. Frontend: shared `UserBadges` (official checkmark then Seeker Genesis badge — real SGT artwork bundled at `assets/badges/seeker-genesis.png`, purple ring, `Seeker Verified` a11y label, profile long-press explainer) replacing per-screen `VerifyBadge`/`BadgeCheck` in profile headers (22, with "Seeker Verified · Genesis Token" line under the REP pill), feed, post details, comments, search, follows, recommendations, collectibles, event connections, chat list/header, and NFC/BLE tap-success cards (22); Settings WALLET section "Verify Seeker" row via `src/api/verify.seeker.ts` (per-error toasts incl. "already linked to another NextVibe account"); `seeker_verified` push type routes to the profile tab after `clearProfileCache()`. One-time `.skr`/wallet-scan bootstrap lives in gitignored `backend/NextVibeAPI/scripts_tmp/grant_seeker_badges.py` (`--dry-run`; delete after running). Files: `backend/NextVibeAPI/user/{models.py,urls.py,admin.py,views_pac/{wallet_singin.py,save_wallet_address.py,seeker_verify.py,search.py,recommended_users.py,history.py,get_readers.py,get_follows.py},src/{seeker_verification.py,send_push_message.py}}`, `posts/{serializers_pac/recommendation_feed_serializer.py,view_pac/{posts_menu.py,comment_create.py,get_post.py,get_comments.py,recomendations.py,get_nfts_menu.py,event_connections.py}}`, `chat/{views.py,views_cherry.py}`, `NextVibeAPI/setting/{dev,prod}.py`, `nft-service/src/index.ts`, `frontend/NextVibe/components/Shared/UserBadges.tsx`, `assets/badges/seeker-genesis.png`, `src/api/verify.seeker.ts`, `components/{ProfilePage/*,Home/MainPage.*,PostDetails/*,Comments/CommentPopup.tsx,Search/SearchPage.*,Chat/{ChatItem,CustomChatScreen}.tsx,Events/EventNFC{Share,Receive}Screen.tsx,Settings/PageSettings.tsx}`, `app/{_layout.tsx,(shared)/u/e.tsx}`, `hooks/useBleScanner.tsx`, `src/{types/post.ts,api/proximity.token.ts}`
- fix(frontend): edit-message websocket event in `CustomChatScreen` builds a `MessageItem` without the required `chat_id` — now set from the active `chatId` (pre-existing tsc error). Files: `frontend/NextVibe/components/Chat/CustomChatScreen.tsx`
- fix(frontend): "View in Wallet" on the collect sheet now opens the in-app wallet Collectibles tab with the minted asset's detail sheet focused (`/wallet-dash?tab=collectibles&asset=<id>`, name/image ride along so DAS indexing lag doesn't block the sheet; Android+MWA keeps a small "Open in Seed Vault Wallet" external link); collectibles grids are responsive 3-column (`useWindowDimensions` + floored tile size, shared square `CollectibleTile` with gradient name overlay — no more dead space on the right on Seeker/wide iPhones, profile grid sizing floored too); dashboard Last Transaction card renders cNFTs like history rows via new shared `NftTxRow` (thumbnail + Collected/Received/Sent/Burned + name, no amount/price, purple accent), skips the token price lookup for `token:'cNFT'`, and tapping it opens the same transaction detail screen with the same params as a history row (shared `buildCnftDetailParams`). Files: `frontend/NextVibe/components/NftClaim/MintBottomSheet/index.tsx`, `components/Wallet/Collectibles/{CollectiblesScreen.tsx,CollectibleTile.tsx}`, `components/Wallet/Dashboard/{DashboardScreen.tsx,LastTransaction.tsx}`, `components/Wallet/Shared/NftTxRow.tsx`, `components/Wallet/TransactionsHistory/TransactionItem.tsx`, `components/ProfilePage/CollectionsMenu.tsx`, `hooks/useLastTransaction.ts`
- fix(frontend): cNFT mints/transfers/burns now appear in in-app wallet history — `parseHeliusTransactions` handles `events.compressed` (claimed/received/sent/burned rows with `token:'cNFT'`), decodes the SPL Memo (v1/v2, base58 or utf-8) via new `extractMemo`, and resolves thumbnails/names from metadata URIs through a 24h in-memory cache (`cnftMetadata.ts`); history rows show kind title + name + thumbnail, detail screen shows large image, memo, network fee ("$0.00 · paid by NextVibe" on the gasless path) and an "Open in Collectibles" deep link; tx-indexer keeps leaf-owner-only transactions (`involvesWallet` checks `events.compressed`) and the Helius webhook now subscribes to `COMPRESSED_NFT_BURN`. Files: `frontend/NextVibe/src/{types/solana.ts,utils/solana/{heliusParser.ts,cnftMetadata.ts}}`, `components/Wallet/TransactionsHistory/{TransactionItem.tsx,TransactionDetailScreen.tsx,TransactionsHistoryScreen.tsx}`, `tx-indexer/src/services/{types.ts,transaction-filter.ts,webhook-manager.ts}`
- feat(frontend): Collectibles tab in the wallet dashboard — `Tokens | Collectibles` segmented header on `DashboardScreen`, 3-column grid backed by Helius DAS `getAssetsByOwner` through the backend RPC proxy (key stays server-side, new 60s per-request Django cache), NextVibe-minted items grouped first with `OG`/`POAP`/`Post` pills, detail sheet with edition, Asset ID (shared `InfoRow` exported from the profile collectible modal), Solscan link and unverified-collection note; deep-linkable via `/wallet-dash?tab=collectibles&asset=<id>`. Files: `frontend/NextVibe/components/Wallet/Collectibles/{useOwnedAssets.ts,CollectiblesScreen.tsx,CollectibleDetailSheet.tsx}`, `components/Wallet/Dashboard/DashboardScreen.tsx`, `components/ProfilePage/CollectiblesModal.tsx`, `backend/NextVibeAPI/wallet/view/rpc_proxy.py`
- feat(frontend): cNFT send staged behind `FEATURE_CNFT_SEND` — flag ships off with a disabled "Send — coming soon" button on the collectible detail sheet; per-platform signing flows and the backend transfer/prepare endpoint land when the flag flips. Files: `frontend/NextVibe/constants/FeatureFlags.ts`, `components/Wallet/Collectibles/CollectibleDetailSheet.tsx`

## 2026-09-09
- fix(frontend): MintBottomSheet — Reanimated-only animations, zero text scaling to eliminate rasterization/pixelation on iOS/Android, shimmering track fill during minting, rewritten SwipeToCollect with proper Gesture Handler offsets (`activeOffsetX(8)`, `failOffsetY([-14, 14])`, pull-to-dismiss sheet pan, single-fire haptic at 85%), and wrapped in `GestureHandlerRootView` across all render paths. Files: `frontend/NextVibe/components/NftClaim/MintBottomSheet/{index.tsx,SwipeToCollect.tsx,useCollectFlow.ts,HeroCard.tsx}`
- perf(backend): respond immediately on collect and offload author push notification to background daemon thread via `transaction.on_commit`; store deterministic `asset_id` on `PendingClaim` during prepare; added `python manage.py backfill_asset_ids` management command to resolve historic unparsed cNFT records. Files: `backend/NextVibeAPI/posts/view_pac/collect.py`, `posts/models.py`, `posts/management/commands/backfill_asset_ids.py`
- perf(nft-service): deterministic asset id via tree `numMinted` PDA derivation under in-process mutex `withMintLock`, `skipPreflight: true` broadcast, dropped the 6-retry delay parse loop in favor of instant response & single-shot confirmed parse; added `POST /asset-id-from-signature` endpoint. End-to-end collect time dropped from ~10–12s to ≤1.8s on iOS and ≤3.5s on MWA. Files: `nft-service/src/index.ts`
- fix(frontend): collect flow selects signer by platform; iOS and passkey users use gasless path, Android+MWA signs memo via Seed Vault; logger dumps raw wallet errors. Backend defensively downgrades `signer:"mwa"` to `"none"` for non-Android clients via the new `X-Client-Platform` header (+ test). Files: `frontend/NextVibe/components/NftClaim/MintBottomSheet/useCollectFlow.ts`, `src/utils/walletLogger.ts`, `src/api/collect.ts`, `backend/NextVibeAPI/posts/view_pac/collect.py`, `posts/tests/test_collect.py`
- chore(all): end-to-end diagnostic logging for the cNFT mint/collect flow — Django `posts.collect` logger (INFO console config in settings) logs every prepare/submit/publish request, every rejection with its error code + user/post context, edition reservations, nft-service failures with bodies, finalization, rep bonus, and push outcomes; nft-service logs timestamped structured events for all endpoints (request, metadata, build/hash, signature verification failures with signer index, send/confirm timing, parse retries, done) and `/mint` now returns clean `{success:false,error}` instead of crashing; frontend `useCollectFlow` logs each flow phase (prepare/sign/submit, silent expiry retry, cancel, mapped errors) via `walletLogger` under a new `Collect:Flow` tag. Serialized transactions are never logged. Files: `backend/NextVibeAPI/{NextVibeAPI/settings.py,posts/view_pac/{collect,mint_nft}.py}`, `nft-service/src/index.ts`, `frontend/NextVibe/{components/NftClaim/MintBottomSheet/useCollectFlow.ts,src/utils/walletLogger.ts}`
- docs(nft-service): README documenting all four endpoints (`/mint`, `/mint/og`, `/collect/prepare`, `/collect/submit`), the user-signed memo mechanism, env vars, and logging policy. Files: `nft-service/README.md`
- test(backend): collect flow test suite — daily-limit boundary, distinct editions for concurrent prepares, IRL reservation enforced/lifted around the 24h window, collect rep bonus excluded from eligibility, claim expiry (410), submit finalization, signer:"none" legacy mint, already-claimed/sold-out/owner guards (12 tests, all passing on sqlite). Removed the stub `posts/tests.py` that shadowed the `posts/tests/` package so the suite is discoverable. Files: `backend/NextVibeAPI/posts/tests/{__init__.py,test_collect.py}`
- chore(repo): add hackathon changelog to track all CLOCK IN work. Files: `CHANGELOG-hackathon.md`
- feat(nft-service): two-phase user-signed free collect — `POST /collect/prepare` builds a gasless mint tx (backend fee payer) with an SPL Memo instruction requiring the collector's signature and partial-signs it; `POST /collect/submit` verifies the message hash and all required signatures, broadcasts, confirms, and parses the cNFT asset ID. Files: `nft-service/src/index.ts`
- feat(frontend): free collect flow — new `src/api/collect.ts` (prepare/submit with typed error codes) and `NftClaim/useCollectFlow.ts` state machine (prepare → MWA `signTransactions` → submit, one silent retry on expiry, LazorKit fallback via signer:"none", owner publish path); MintBottomSheet stripped of all pricing UI ("≈ free (network fee only)", edition counter, IRL reservation note, mapped error copy); SOL-transfer payment builder deleted; feed/detail/profile screens rewired to `collect`/`onCollected`. Files: `frontend/NextVibe/src/api/{collect,mint.nft}.ts`, `components/NftClaim/{MintBottomSheet.tsx,ButtonCollect.tsx,useCollectFlow.ts}`, `components/{Home/MainPage.android.tsx,Home/MainPage.ios.tsx,PostDetails/PostDetailsScreen.tsx,ProfilePage/PostModal.tsx,Events/AddLumaEventSheet.tsx}`, `src/types/post.ts` (removed `hooks/buildPaymentInstructions.ts`)
- feat(frontend): MintBottomSheet redesigned as a collect-card experience — tilted floating hero card with gradient border/glow that flips to a shimmer back while minting, 50-dot edition strip (minted/IRL-reserved/free, pulsing upcoming edition), "what you get" pills, gradient swipe track with "Release to collect", success particle burst + count-up chip + haptics, "View in wallet"/"Done" actions, `reduceMotion` respected; refactored into `NftClaim/MintBottomSheet/` (index, HeroCard, EditionStrip, SwipeToCollect, SuccessBurst, useCollectFlow). Files: `frontend/NextVibe/components/NftClaim/MintBottomSheet/*`, caller screens updated for `creatorAvatar`
- feat(backend): free collect endpoints — `PendingClaim` model reserves editions for in-flight claims; `POST /posts/collect/prepare/` enforces wallet/ownership/duplicate checks, a 10-claims-per-UTC-day limit, 50-edition supply cap, and a 24h IRL reservation of editions 2–11 (networking tap or event check-in with the author); `POST /posts/collect/submit/` finalizes the user-signed mint, awards +2 rep to IRL-connected claimers, and pushes the author. Owner publish path (`cnft-mint/`) stripped of price/payment handling; post serializers now expose a `collect` object and no longer serialize prices. Files: `backend/NextVibeAPI/posts/{models,constants,urls}.py`, `posts/src/collect_{eligibility,memo}.py`, `posts/view_pac/{collect,mint_nft,get_post,recommendation_feed}.py`, `posts/serializers_pac/recommendation_feed_serializer.py`
